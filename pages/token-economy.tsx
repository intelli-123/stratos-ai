import Stub from "@/components/Stub";
export default function TokenEconomy() { return <Stub title="Token Economy" sub="Tokens & cost by agent, team and model" points={["Tokens consumed per agent and per query","Cost per query and per agent vs budget","Spend trend over time and top spenders"]} />; }
