import Stub from "@/components/Stub";
export default function Traces() { return <Stub title="Traces" sub="Per-query traces across agents" points={["Each LLM call / tool call as a span (model, tokens, latency)","Filter by agent, model, status","Drill into a single query's full trace"]} />; }
