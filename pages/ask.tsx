import Stub from "@/components/Stub";
import { APP_NAME } from "@/lib/app";
export default function Ask() { return <Stub title={`Ask ${APP_NAME}`} sub="Natural-language questions over your fleet" points={["Ask about spend, usage, and incidents","Summaries and recommendations","Backed by your live agent data"]} />; }
