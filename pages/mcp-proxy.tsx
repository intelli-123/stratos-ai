import Stub from "@/components/Stub";
export default function McpProxy() { return <Stub title="MCP Proxy" sub="Live MCP tool-call metering" points={["MCP servers connected via the proxy","Per-tool call counts, tokens, latency","Local / remote MCP inventory"]} />; }
